# ============================================================================
# Configuration Dashboard Bexio → Power BI
# ============================================================================

# API Bexio
# Obtenez votre token sur: https://office.bexio.com → Paramètres → Intégrations → API
BEXIO_API_TOKEN=votre_token_api_ici
BEXIO_ORGANIZATION_ID=votre_organization_id

# Configuration de l'extraction
# Endpoints à extraire (séparés par des virgules)
# Options: contacts, invoices, quotes, orders, projects, timesheets, articles
BEXIO_ENDPOINTS=contacts,invoices,quotes,projects

# Nombre de jours à extraire dans le passé (pour les factures, devis, etc.)
EXTRACTION_DAYS=365

# ============================================================================
# Notifications par Email (Optionnel)
# ============================================================================

# Activer les notifications email (true/false)
EMAIL_NOTIFICATIONS=false

# Destinataire(s) (séparés par des virgules)
EMAIL_TO=votre.email@example.com

# Configuration SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre.email@gmail.com
SMTP_PASSWORD=votre_mot_de_passe_app

# Note: Pour Gmail, utilisez un "mot de passe d'application"
# https://support.google.com/accounts/answer/185833

# ============================================================================
# Configuration Avancée (Optionnel)
# ============================================================================

# Profil utilisateur (pour optimisations)
USER_SECTOR=1
CLIENTS_PER_YEAR=50

# Gestion multi-clients
# Si défini, utilise le fichier configs/{CLIENT}.env
# CLIENT=nom_du_client
